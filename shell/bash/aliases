# shellcheck shell=bash

# Small aliases for Bash fallback sessions.
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

if [ "$(uname -s)" = Darwin ]; then
    alias ls='ls -1 -haFG'
else
    alias ls='ls -1 -haF --color=auto'
fi

if command -v bat >/dev/null 2>&1; then
    alias cat='bat'
elif command -v batcat >/dev/null 2>&1; then
    alias cat='batcat'
fi

alias activate='source .venv/bin/activate'
alias weather='curl wttr.in'
command -v python3 >/dev/null 2>&1 && alias p='python3'
alias c='claude'

alias g='git'
alias gs='git status'
alias ga='git add .'
alias gd='git diff'
alias gc='git commit -m'
alias gb='git branch'
alias gk='git checkout'
alias gks='git checkout staging'
alias gkm='git checkout main'
alias gm='git merge'
alias gl='git log --oneline --decorate --graph'
alias ghr='git reset --hard HEAD'
alias gsh='git push'
alias gll='git pull'
alias gf='git fetch'
alias grp='git rev-parse --short HEAD'

alias ruffd='ruff check --fix --select I && ruff format'
alias brd='bun run dev'
alias nrd='bun run dev'
alias rnd='bun run dev'
alias brb='bun run build'
alias brs='bun run start'

if [ "$(uname -s)" = Darwin ]; then
    alias sublime="open -a 'Sublime Text'"
    alias tailscale='/Applications/Tailscale.app/Contents/MacOS/Tailscale'
fi
